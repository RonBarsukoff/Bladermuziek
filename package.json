{
  "name": "bladermuziek",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "npx tsc --listFiles", 
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
